# nvidia-gsp-updater
Automatically keeps NVIDIA GSP firmware paths in sync in your dracut config on AlmaLinux 10.x and RHEL 10 derivatives, DNF hook regenerates initramfs on every driver install, update, or removall, no manual editing. 
